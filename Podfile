# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestiOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'fishhook', '~> 0.2'

  # Pods for TestiOS

  target 'TestiOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestiOSUITests' do
    # Pods for testing
  end

end
